# Исследовательский README: Проект по мультимодальному анализу выживаемости (бинарная классификация)
Этот документ служит живым журналом исследования. Он структурирует все этапы работы, фиксирует ключевые решения, результаты и выводы. 

## 1. Общая информация о проекте
Название проекта: Разработка методов обработки мультимодальных данных для прогнозирования неблагоприятных событий больных инфрактом миокарда

Исследователь (автор):
Мельник Дмитрий, lemnik27@gmail.com

Дата начала исследования:
01.09.2025
Последнее обновление:
16.03.2026

Цели исследования:

*Сформулируйте основную цель (например, разработка модели бинарной классификации для предсказания 5-летней выживаемости пациентов на основе табличных клинических данных).*

Какие вспомогательные задачи решаются?

Гипотезы:

Например: «Комбинация клинических признаков X и Y позволяет достичь AUC > 0.8».

Какие предположения о данных или модели проверяются?

Клиническая/научная значимость:

Почему эта задача важна? Кому будут полезны результаты?

Возможное влияние на практику или дальнейшие исследования.

## 2. Данные
### 2.1. Источник данных
Клинический больничный датасет.

Ссылки на источник, DOI, лицензионные ограничения.

Дата выгрузки/получения.

### 2.2. Описание набора данных
Краткая характеристика: количество записей, количество признаков, тип данных.

Таблица с описанием переменных:

| Признак | Тип | Описание | Единицы измерения | Возможные значения | Пропуски (%) | 
| --- | --- | --- | --- | --- | --- |
| Ячейка 1 | Ячейка 2 | Ячейка 3 | Ячейка 1 | Ячейка 2 | Ячейка 3 |
| Ячейка 1 | Ячейка 2 | Ячейка 3 | Ячейка 1 | Ячейка 2 | Ячейка 3 |

...	...	...	...	...	...
### 2.3. Целевая переменная
Определение: что считается положительным классом (событие: смерть/рецидив в течение определённого времени)?

Как получена метка (например, по дате смерти из регистра)?

*Бинарная метка: 0 – выжил, 1 – не выжил / событие.*

*Временной горизонт (если применимо): например, 5-летняя выживаемость.*

### 2.4. Размер выборки и баланс классов
Общее количество пациентов.

Количество положительных / отрицательных примеров.

Доля положительного класса.

Баланс/дисбаланс классов.

### 2.5. Планируемая мультимодальность (на будущее)
Какие ещё модальности планируется добавить (изображения, тексты отчётов, геномные данные)?

Как они могут быть интегрированы с текущими табличными данными?

## 3. Предобработка данных
### 3.1. Очистка данных
Обработка пропусков: какие стратегии использованы (удаление, вменение средним/медианой/модой, модели предсказания)?

Выявление и обработка выбросов (методы, пороги).

Удаление дубликатов, проверка консистентности (например, дат).

### 3.2. Кодирование категориальных признаков
Какие признаки закодированы и каким методом (One-Hot, Label, Target encoding, ...)?

Обоснование выбора.

### 3.3. Масштабирование признаков
Применялось ли масштабирование (StandardScaler, MinMaxScaler, RobustScaler)?

Для каких моделей это необходимо.

### 3.4. Создание новых признаков (Feature Engineering)
Список созданных признаков, формула или метод.

Например, индекс массы тела, отношение признаков, полиномиальные признаки.

Биологически/клинически обоснованные комбинации.

### 3.5. Балансировка классов (если применялась)
Методы: взвешивание классов, Oversampling (SMOTE, ADASYN), Undersampling.

Применялось ли до или после разделения на выборки?

### 3.6. Разделение на обучающую, валидационную и тестовую выборки
*Пропорции разделения (например, 70/15/15).*

Стратификация по целевой переменной (для сохранения распределения классов).

Учтён ли временной аспект (например, разделение по дате поступления)?

Фиксация random_state для воспроизводимости.

## 4. Разведочный анализ данных (EDA)
### 4.1. Статистические описания
Основные статистики для числовых признаков (среднее, медиана, std, мин/макс, квартили).

Частотные таблицы для категориальных признаков.

### 4.2. Визуализации
Гистограммы/распределения признаков.

Box-plot для выявления выбросов.

Pairplot/scatter matrix (для ограниченного числа признаков).

Тепловая карта корреляций (Pearson/Spearman).

Сравнение распределений признаков в группах по целевой переменной.

### 4.3. Анализ пропусков
Матрица пропусков, визуализация (например, missingno).

Зависимость пропусков от других признаков или целевой переменной.

### 4.4. Выводы из EDA
Ключевые наблюдения (сильно скошенные признаки, мультиколлинеарность, важные корреляции с целевой переменной).

Какие признаки могут быть наиболее информативными?

Необходимость дополнительной обработки (например, преобразования для уменьшения скошенности).

## 5. Моделирование
### 5.1. Базовые модели (Baselines)
Простые модели для сравнения: DummyClassifier (стратифицированный, most frequent), логистическая регрессия, решающее дерево.

Результаты baseline (метрики на валидации).

### 5.2. Основные модели
Перечень моделей, которые планируется/было опробовано:

Random Forest

XGBoost / LightGBM / CatBoost

SVM

Нейронные сети (MLP)

Ансамбли (стекинг, блендинг)

Для каждой модели указать:

Используемая реализация (библиотека, версия).

Конфигурация (ключевые гиперпараметры по умолчанию).

Время обучения.

### 5.3. Настройка гиперпараметров
Метод поиска: Grid Search, Random Search, Bayesian Optimization (Optuna, Hyperopt).

Пространство поиска (какие параметры варьировались, диапазоны).

Критерий оптимизации (например, максимизация ROC-AUC на валидации).

Лучшие найденные параметры для каждой модели.

### 5.4. Обучение и валидация
Схема валидации: кросс-валидация (k-fold, stratified k-fold) или отложенная выборка.

Фиксация random_state.

Таблица с результатами кросс-валидации (среднее и std метрик).

### 5.5. Интерпретация моделей
Важность признаков (feature importance) для древовидных моделей.

SHAP-анализ (summary plot, dependence plot).

LIME для локального объяснения предсказаний.

Какие выводы о влиянии признаков получены?

## 6. Оценка и метрики
### 6.1. Выбранные метрики
*Основные метрики: AUC-ROC, Accuracy, Precision, Recall (Sensitivity), F1-score, Specificity.*

Обоснование выбора в контексте задачи (например, важность чувствительности для выявления пациентов высокого риска).

### 6.2. Пороги классификации
Используемый порог по умолчанию (0.5) или оптимизированный.

*Если порог подбирался, то по какой метрике (например, максимизация F1, компромисс sensitivity/specificity).*

### 6.3. Доверительные интервалы
Рассчитывались ли доверительные интервалы для метрик (бутстреп)?

*Результаты (например, AUC 0.85 [0.82-0.88]).*

### 6.4. Сравнение моделей
Сводная таблица метрик на валидационной и тестовой выборках:

| Модель | ROC-AUC | Accuracy | Precision | Recall | F1 | Specificity |   
| --- | --- | --- | --- | --- | --- | --- |
| Ячейка 1 | Ячейка 2 | Ячейка 3 | Ячейка 1 | Ячейка 2 | Ячейка 3 | Ячейка 3 |
| Ячейка 1 | Ячейка 2 | Ячейка 3 | Ячейка 1 | Ячейка 2 | Ячейка 3 | Ячейка 3 |

Графики ROC-кривых, Precision-Recall кривых (если дисбаланс).

### 6.5. Анализ ошибок
Confusion matrix для лучшей модели.

Анализ ошибочно классифицированных примеров: какие признаки характерны для ложноположительных/ложноотрицательных?

Проверка на подгруппах (например, по возрасту, полу, стадии заболевания).

## 7. Результаты и выводы
### 7.1. Итоговые метрики лучшей модели на тестовой выборке
Зафиксируйте финальные цифры.

Сравнение с baseline.

### 7.2. Интерпретация результатов
Какие признаки оказались наиболее важными? Совпадает ли это с клиническими ожиданиями?

Какие ограничения выявлены (например, модель плохо работает на определённой подгруппе)?

Устойчивость модели (проверка на разных разбиениях).

### 7.3. Клиническая значимость
Насколько полученная модель может быть полезна в практике (например, для скрининга, поддержки принятия решений)?

Необходимость внешней валидации на независимой когорте.

### 7.4. Ограничения исследования
Размер выборки, качество данных, отсутствие некоторых важных признаков, потенциальный перекос (bias).

Что может улучшить достоверность выводов?

## 8. Планы и дальнейшие шаги
### 8.1. Улучшение модели
Какие ещё эксперименты с моделями планируются?

Использование более сложных архитектур (например, нейросети с вниманием).

Учёт временных зависимостей (анализ выживаемости вместо бинарной классификации).

### 8.2. Добавление новых данных / модальностей
Какие данные планируется собрать/подключить?

Как будет организована интеграция мультимодальных данных (ранняя, поздняя, гибридная)?

### 8.3. Подготовка публикации
Целевой журнал / конференция.

План статьи: основные результаты, графики, таблицы.

Необходимые дополнительные эксперименты для рецензентов.

### 8.4. Развертывание (если применимо)
Создание веб-сервиса, API, интеграция в электронные медицинские карты.

Вопросы безопасности, регуляторики (если планируется клиническое использование).

## 9. Организация проекта
### 9.1. Структура репозитория
```bash
text
project/
├── data/               # сырые и обработанные данные (в .gitignore)
├── notebooks/          # Jupyter ноутбуки с EDA, экспериментами
├── src/                # исходный код (модули для загрузки, предобработки, моделей)
├── models/             # сохранённые обученные модели
├── reports/            # отчёты, фигуры, таблицы
├── README.md           # этот файл
├── requirements.txt    # зависимости
└── .gitignore
```
### 9.2. Воспроизводимость
Инструкция по установке зависимостей (pip install -r requirements.txt).

Фиксированные версии библиотек.

Как запустить основной пайплайн (например, python src/run_pipeline.py).

Ссылки на ключевые ноутбуки и их порядок запуска.

### 9.3. Используемые инструменты и библиотеки
Python, Jupyter, Pandas, NumPy, Scikit-learn, XGBoost, LightGBM, CatBoost, SHAP, Matplotlib, Seaborn, Optuna и т.д.

## 10. Ссылки и литература
Список статей, датасетов, блогов, которые были использованы или вдохновили.

Формат: автор(ы), название, год, ссылка/DOI.

Ссылки на документацию библиотек.

## 11. Журнал изменений (Changelog)
Фиксируйте ключевые этапы, даты и изменения в исследовании.

| Дата | Версия | Изменения | Автор |  
| --- | --- | --- | --- | 
| 16.03.2026 | 0.0.1 | Создание шаблона и сохранение всей текущей работы | Мельник Д.Б. 

...	...	...	...
Примечания и напоминания
Регулярно обновляйте этот документ.

Сохраняйте все промежуточные результаты (метрики, графики) в папке reports.

Используйте контроль версий (git) для кода и этого README.

Для каждой экспериментируемой модели фиксируйте гиперпараметры и random seed.

Если используете облачные сервисы или кластер, делайте пометки о вычислительных ресурсах.